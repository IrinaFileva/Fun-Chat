import { Input } from './input';

export const inputCreate = new Input('input', 'input-create input').setAttributeButton({ type: 'text' });

export const inputUpdate = new Input('input', 'input-update input').setAttributeButton({ type: 'text' });
